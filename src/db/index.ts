import { items } from "./item";
import { logs } from "./log";
import { reviews } from "./review";
import { users } from "./user";
const db = {
  users,
  items,
  logs,
  reviews,
};

export default db;
